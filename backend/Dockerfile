FROM rust:1.88 AS builder

WORKDIR /usr/src/app

COPY Cargo.toml Cargo.lock ./
COPY src ./src

RUN cargo build --release

# Runtime stage
FROM debian:bookworm-slim

RUN apt-get update && apt-get install -y \
    ca-certificates \
    libssl3 \
    && rm -rf /var/lib/apt/lists/*

COPY --from=builder /usr/src/app/target/release/omni-api /usr/local/bin/omni-api
COPY --from=builder /usr/src/app/target/release/omni-worker /usr/local/bin/omni-worker

EXPOSE 8080

CMD ["omni-api"]
